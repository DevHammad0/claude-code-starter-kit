# Frontend — [Framework]

## Entry Points
```bash
# Replace with your actual commands
pnpm install
pnpm dev
pnpm tsc --noEmit
pnpm playwright test
```

## Structure
```
# Replace with your actual structure
app/
├── (auth)/          # auth pages
├── (main)/          # main app pages
└── api/             # API routes (if any)
components/
├── ui/              # base UI components
└── [feature]/       # feature components
lib/
├── api.ts           # backend client
└── auth.ts          # auth client
```

## Key Constraints
- [List your non-negotiable frontend rules here]
- [E.g.: Server Components by default, "use client" only when needed]
- [E.g.: No inline styles — Tailwind only]
- [E.g.: No any types — strict TypeScript]
